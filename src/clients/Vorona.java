package clients;
import java.time.LocalDate;
public class Vorona extends Animal {
    public Vorona(String name, float weight, LocalDate age, Owner owner) {
        super(name, weight, age, owner);
    }
}
